#include "main.h"
#include <string.h>
/**
 *  _strlen -> function to get the length of a string
 * @a:
 * string pointer to passed to thid function
 *  Returns length of the string
 */
int _strlen(char *s)
{
	int len;

	for (; *s != '\0'; s++)
	{
	len + = 1;	

	return (strlen(s));
}
